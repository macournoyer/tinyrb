%{
#include <stdlib.h>    /* atoi() */
#include "tr.h"
#include "internal.h"
%}

Value   = i:NUMBER    { printf("%d\n", i); }

NUMBER  = < [0-9]+ >  { $$ = atoi(yytext); }

%%

TrBlock *TrBlock_compile(VM, char *code, char *fn, size_t lineno) {
  while (yyparse())
    ;
  exit(0);
  return 0;
}
